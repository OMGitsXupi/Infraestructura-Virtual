# docker-compose.yml
version: '2'

services:
   wikirandom:
      build:
        context: .
        dockerfile: Dockerfile
      volumes:
        - .:/usr/src/wikirandom
      working_dir: /usr/src/wikirandom
